import { AppointmentCountryService } from "./appointment.country.service";

/**
 * Implementación del servicio de citas para Chile.
 * Extiende la funcionalidad base del servicio de citas por país,
 * dejando la posibilidad de agregar operaciones específicas para Chile.
 */
export class AppointmentCLService extends AppointmentCountryService {}
